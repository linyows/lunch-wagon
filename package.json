{
  "name": "lunch-wagon",
  "version": "2.0.0",
  "description": "Notify lunch members from slack usergroup random selects",
  "scripts": {
    "ci": "npm i && npm run lint",
    "lint": "tslint src/*.ts",
    "lint:fix": "tslint --fix src/*.ts"
  },
  "devDependencies": {
    "@google/clasp": "^2.0.0",
    "tslint": "^5.11.0",
    "tslint-microsoft-contrib": "^6.0.0"
  },
  "tslintIgnore": [
    "**/node_modules/**"
  ],
  "author": "linyows",
  "license": "MIT",
  "homepage": "https://github.com/linyows/lunch-wagon.gs",
  "dependencies": {
    "@types/google-apps-script": "1.0.0"
  }
}
